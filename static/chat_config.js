/**
 * Demo chat — change API base URL here only (no secrets).
 *
 * - "" (empty string) = same origin as this page (typical: local uvicorn).
 * - Remote example: "https://your-app.vercel.app" (no trailing slash).
 */
window.CHATBOT_API_BASE = "";
